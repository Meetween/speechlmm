# @package _global_

defaults:
  - _self_

wandb_project: runs_paper_v1_0

training:
  num_train_epochs: 2
  per_device_train_batch_size: 10
  per_device_eval_batch_size: 4
  gradient_accumulation_steps: 2
  report_to: tensorboard
